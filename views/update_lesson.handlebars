<!DOCTYPE html>
<html lang="en">
    {{#section 'head'}}
    <style>
        textarea{
            font-size:20px;
            font-family:'Montserrat Alternates';
            border-radius:10px;outline:none;
            transition: box-shadow .3s ease
        }

        textarea:-ms-input-placeholder {color: #fd7e14}
        textarea::-moz-placeholder {color: #fd7e14}
        textarea::-webkit-input-placeholder {color: #fd7e14}

        textarea:focus{2px solid #fd7e14}
        textarea:focus,
        textarea:hover{box-shadow:0px 0px 13px 0px  #ffc107}
                 
        
    </style>
    {{/section}}
  <body>
    <h1>Update Lesson</h1>
        <form class="form-area" action="/update_lesson?id={{lesson_id}}" method="POST">
            <div class="container">
            <input style="margin-bottom: 10px;" name="title" type="text" value="{{title}}">
            <textarea id="field" name="postText" rows="20" cols="70">{{body}}</textarea>
            <br>
            <button type="submit" class="btn btn-primary">Update</button>
            </div>
        </form>
  </body>
</html>